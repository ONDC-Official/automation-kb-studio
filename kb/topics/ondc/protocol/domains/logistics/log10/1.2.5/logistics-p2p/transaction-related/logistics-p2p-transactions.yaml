# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: logistics-p2p-transactions
path:
  - ondc
  - protocol
  - domains
  - logistics
  - log10
  - "1.2.5"
  - logistics-p2p
  - transaction-related
title: 'ONDC:LOG10 1.2.5 — Logistics (P2P) transactions'
kind: real
questions:
  - 'What is the transaction flow for the Logistics (P2P) use case in ONDC:LOG10 version 1.2.5?'
  - 'How does the transaction flow work for the Logistics (P2P) use case in ONDC:LOG10 version 1.2.5?'
  - 'What are the transaction steps for the Logistics (P2P) use case in ONDC:LOG10 version 1.2.5?'
