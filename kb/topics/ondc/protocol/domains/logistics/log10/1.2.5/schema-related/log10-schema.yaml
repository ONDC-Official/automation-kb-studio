# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: log10-schema
path:
  - ondc
  - protocol
  - domains
  - logistics
  - log10
  - "1.2.5"
  - schema-related
title: 'ONDC:LOG10 1.2.5 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:LOG10 version 1.2.5?'
  - 'How is the data schema defined for ONDC:LOG10 version 1.2.5?'
  - 'What schema does ONDC:LOG10 version 1.2.5 use?'
