# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: agriculture-input-transactions
path:
  - ondc
  - protocol
  - domains
  - agriculture
  - agr10
  - "1.2.5"
  - agriculture-input
  - transaction-related
title: 'ONDC:AGR10 1.2.5 — agriculture-input transactions'
kind: real
questions:
  - 'What is the transaction flow for the agriculture-input use case in ONDC:AGR10 version 1.2.5?'
  - 'How does the transaction flow work for the agriculture-input use case in ONDC:AGR10 version 1.2.5?'
  - 'What are the transaction steps for the agriculture-input use case in ONDC:AGR10 version 1.2.5?'
