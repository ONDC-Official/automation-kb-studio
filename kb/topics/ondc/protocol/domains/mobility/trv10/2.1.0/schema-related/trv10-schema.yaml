# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv10-schema
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv10
  - "2.1.0"
  - schema-related
title: 'ONDC:TRV10 2.1.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:TRV10 version 2.1.0?'
  - 'How is the data schema defined for ONDC:TRV10 version 2.1.0?'
  - 'What schema does ONDC:TRV10 version 2.1.0 use?'
