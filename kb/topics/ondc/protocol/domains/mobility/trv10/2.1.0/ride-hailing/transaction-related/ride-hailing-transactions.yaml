# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ride-hailing-transactions
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv10
  - "2.1.0"
  - ride-hailing
  - transaction-related
title: 'ONDC:TRV10 2.1.0 — Ride-hailing transactions'
kind: real
questions:
  - 'What is the transaction flow for the Ride-hailing use case in ONDC:TRV10 version 2.1.0?'
  - 'How does the transaction flow work for the Ride-hailing use case in ONDC:TRV10 version 2.1.0?'
  - 'What are the transaction steps for the Ride-hailing use case in ONDC:TRV10 version 2.1.0?'
