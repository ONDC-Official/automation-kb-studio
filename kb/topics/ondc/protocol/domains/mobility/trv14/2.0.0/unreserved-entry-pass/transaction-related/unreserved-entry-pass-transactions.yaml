# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: unreserved-entry-pass-transactions
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv14
  - "2.0.0"
  - unreserved-entry-pass
  - transaction-related
title: 'ONDC:TRV14 2.0.0 — unreserved-entry-pass transactions'
kind: real
questions:
  - 'What is the transaction flow for the unreserved-entry-pass use case in ONDC:TRV14 version 2.0.0?'
  - 'How does the transaction flow work for the unreserved-entry-pass use case in ONDC:TRV14 version 2.0.0?'
  - 'What are the transaction steps for the unreserved-entry-pass use case in ONDC:TRV14 version 2.0.0?'
