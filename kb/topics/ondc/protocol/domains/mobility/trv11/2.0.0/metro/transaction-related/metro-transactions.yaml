# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: metro-transactions
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv11
  - "2.0.0"
  - metro
  - transaction-related
title: 'ONDC:TRV11 2.0.0 — Metro transactions'
kind: real
questions:
  - 'What is the transaction flow for the Metro use case in ONDC:TRV11 version 2.0.0?'
  - 'How does the transaction flow work for the Metro use case in ONDC:TRV11 version 2.0.0?'
  - 'What are the transaction steps for the Metro use case in ONDC:TRV11 version 2.0.0?'
