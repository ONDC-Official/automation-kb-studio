# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv11-error-codes
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv11
  - "2.1.0"
  - error-code-related
title: 'ONDC:TRV11 2.1.0 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:TRV11 version 2.1.0?'
  - 'Which error codes can ONDC:TRV11 version 2.1.0 return?'
  - 'What are the error codes used in ONDC:TRV11 version 2.1.0?'
