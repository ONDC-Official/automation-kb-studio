# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv13-error-codes
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv13
  - "2.0.1"
  - error-code-related
title: 'ONDC:TRV13 2.0.1 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:TRV13 version 2.0.1?'
  - 'Which error codes can ONDC:TRV13 version 2.0.1 return?'
  - 'What are the error codes used in ONDC:TRV13 version 2.0.1?'
