# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv13-business-rules
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv13
  - "2.0.1"
  - business-related
title: 'ONDC:TRV13 2.0.1 — business rules'
kind: real
questions:
  - 'What are the business rules for ONDC:TRV13 version 2.0.1?'
  - 'Which business rules apply to ONDC:TRV13 version 2.0.1?'
  - 'What business rules govern ONDC:TRV13 version 2.0.1?'
