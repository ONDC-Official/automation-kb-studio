# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: airline-transactions
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv12
  - "2.0.0"
  - airline
  - transaction-related
title: 'ONDC:TRV12 2.0.0 — Airline transactions'
kind: real
questions:
  - 'What is the transaction flow for the Airline use case in ONDC:TRV12 version 2.0.0?'
  - 'How does the transaction flow work for the Airline use case in ONDC:TRV12 version 2.0.0?'
  - 'What are the transaction steps for the Airline use case in ONDC:TRV12 version 2.0.0?'
