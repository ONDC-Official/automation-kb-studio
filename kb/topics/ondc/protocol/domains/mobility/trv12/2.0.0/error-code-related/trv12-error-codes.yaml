# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv12-error-codes
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv12
  - "2.0.0"
  - error-code-related
title: 'ONDC:TRV12 2.0.0 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:TRV12 version 2.0.0?'
  - 'Which error codes can ONDC:TRV12 version 2.0.0 return?'
  - 'What are the error codes used in ONDC:TRV12 version 2.0.0?'
