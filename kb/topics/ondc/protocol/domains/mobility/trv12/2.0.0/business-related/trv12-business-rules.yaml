# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv12-business-rules
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv12
  - "2.0.0"
  - business-related
title: 'ONDC:TRV12 2.0.0 — business rules'
kind: real
questions:
  - 'What are the business rules for ONDC:TRV12 version 2.0.0?'
  - 'Which business rules apply to ONDC:TRV12 version 2.0.0?'
  - 'What business rules govern ONDC:TRV12 version 2.0.0?'
