# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: trv12-schema
path:
  - ondc
  - protocol
  - domains
  - mobility
  - trv12
  - "2.0.0"
  - schema-related
title: 'ONDC:TRV12 2.0.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:TRV12 version 2.0.0?'
  - 'How is the data schema defined for ONDC:TRV12 version 2.0.0?'
  - 'What schema does ONDC:TRV12 version 2.0.0 use?'
