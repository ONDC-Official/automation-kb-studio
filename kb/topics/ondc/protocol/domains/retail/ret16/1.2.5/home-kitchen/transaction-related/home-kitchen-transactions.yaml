# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: home-kitchen-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret16
  - "1.2.5"
  - home-kitchen
  - transaction-related
title: 'ONDC:RET16 1.2.5 — HOME&KITCHEN transactions'
kind: real
questions:
  - 'What is the transaction flow for the HOME&KITCHEN use case in ONDC:RET16 version 1.2.5?'
  - 'How does the transaction flow work for the HOME&KITCHEN use case in ONDC:RET16 version 1.2.5?'
  - 'What are the transaction steps for the HOME&KITCHEN use case in ONDC:RET16 version 1.2.5?'
