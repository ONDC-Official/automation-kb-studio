# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: f-b-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret11
  - "1.2.0"
  - f-b
  - transaction-related
title: 'ONDC:RET11 1.2.0 — F&B transactions'
kind: real
questions:
  - 'What is the transaction flow for the F&B use case in ONDC:RET11 version 1.2.0?'
  - 'How does the transaction flow work for the F&B use case in ONDC:RET11 version 1.2.0?'
  - 'What are the transaction steps for the F&B use case in ONDC:RET11 version 1.2.0?'
