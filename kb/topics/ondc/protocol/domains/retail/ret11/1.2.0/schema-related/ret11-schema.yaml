# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ret11-schema
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret11
  - "1.2.0"
  - schema-related
title: 'ONDC:RET11 1.2.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:RET11 version 1.2.0?'
  - 'How is the data schema defined for ONDC:RET11 version 1.2.0?'
  - 'What schema does ONDC:RET11 version 1.2.0 use?'
