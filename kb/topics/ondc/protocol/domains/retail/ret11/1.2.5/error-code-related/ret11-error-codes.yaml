# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ret11-error-codes
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret11
  - "1.2.5"
  - error-code-related
title: 'ONDC:RET11 1.2.5 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:RET11 version 1.2.5?'
  - 'Which error codes can ONDC:RET11 version 1.2.5 return?'
  - 'What are the error codes used in ONDC:RET11 version 1.2.5?'
