# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: bpc-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret13
  - "1.2.0"
  - bpc
  - transaction-related
title: 'ONDC:RET13 1.2.0 — BPC transactions'
kind: real
questions:
  - 'What is the transaction flow for the BPC use case in ONDC:RET13 version 1.2.0?'
  - 'How does the transaction flow work for the BPC use case in ONDC:RET13 version 1.2.0?'
  - 'What are the transaction steps for the BPC use case in ONDC:RET13 version 1.2.0?'
