# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ret10-business-rules
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret10
  - "1.2.5"
  - business-related
title: 'ONDC:RET10 1.2.5 — business rules'
kind: real
questions:
  - 'What are the business rules for ONDC:RET10 version 1.2.5?'
  - 'Which business rules apply to ONDC:RET10 version 1.2.5?'
  - 'What business rules govern ONDC:RET10 version 1.2.5?'
