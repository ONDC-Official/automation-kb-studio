# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: grocery-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret10
  - "1.2.0"
  - grocery
  - transaction-related
title: 'ONDC:RET10 1.2.0 — GROCERY transactions'
kind: real
questions:
  - 'What is the transaction flow for the GROCERY use case in ONDC:RET10 version 1.2.0?'
  - 'How does the transaction flow work for the GROCERY use case in ONDC:RET10 version 1.2.0?'
  - 'What are the transaction steps for the GROCERY use case in ONDC:RET10 version 1.2.0?'
