# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: reteb2b-schema
path:
  - ondc
  - protocol
  - domains
  - retail
  - reteb2b
  - "1.2.5"
  - schema-related
title: 'ONDC:RETeB2B 1.2.5 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:RETeB2B version 1.2.5?'
  - 'How is the data schema defined for ONDC:RETeB2B version 1.2.5?'
  - 'What schema does ONDC:RETeB2B version 1.2.5 use?'
