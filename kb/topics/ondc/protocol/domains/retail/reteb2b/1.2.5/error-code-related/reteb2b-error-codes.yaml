# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: reteb2b-error-codes
path:
  - ondc
  - protocol
  - domains
  - retail
  - reteb2b
  - "1.2.5"
  - error-code-related
title: 'ONDC:RETeB2B 1.2.5 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:RETeB2B version 1.2.5?'
  - 'Which error codes can ONDC:RETeB2B version 1.2.5 return?'
  - 'What are the error codes used in ONDC:RETeB2B version 1.2.5?'
