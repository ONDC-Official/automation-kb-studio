# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: eb2b-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - reteb2b
  - "1.2.5"
  - eb2b
  - transaction-related
title: 'ONDC:RETeB2B 1.2.5 — eB2B transactions'
kind: real
questions:
  - 'What is the transaction flow for the eB2B use case in ONDC:RETeB2B version 1.2.5?'
  - 'How does the transaction flow work for the eB2B use case in ONDC:RETeB2B version 1.2.5?'
  - 'What are the transaction steps for the eB2B use case in ONDC:RETeB2B version 1.2.5?'
