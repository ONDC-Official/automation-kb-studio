# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ret15-schema
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret15
  - "1.2.5"
  - schema-related
title: 'ONDC:RET15 1.2.5 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:RET15 version 1.2.5?'
  - 'How is the data schema defined for ONDC:RET15 version 1.2.5?'
  - 'What schema does ONDC:RET15 version 1.2.5 use?'
