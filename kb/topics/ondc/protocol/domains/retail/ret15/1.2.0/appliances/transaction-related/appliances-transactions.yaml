# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: appliances-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret15
  - "1.2.0"
  - appliances
  - transaction-related
title: 'ONDC:RET15 1.2.0 — APPLIANCES transactions'
kind: real
questions:
  - 'What is the transaction flow for the APPLIANCES use case in ONDC:RET15 version 1.2.0?'
  - 'How does the transaction flow work for the APPLIANCES use case in ONDC:RET15 version 1.2.0?'
  - 'What are the transaction steps for the APPLIANCES use case in ONDC:RET15 version 1.2.0?'
