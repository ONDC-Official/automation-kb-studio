# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: retinvl-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - retinvl
  - "1.2.5"
  - retinvl
  - transaction-related
title: 'ONDC:RETINVL 1.2.5 — RETINVL transactions'
kind: real
questions:
  - 'What is the transaction flow for the RETINVL use case in ONDC:RETINVL version 1.2.5?'
  - 'How does the transaction flow work for the RETINVL use case in ONDC:RETINVL version 1.2.5?'
  - 'What are the transaction steps for the RETINVL use case in ONDC:RETINVL version 1.2.5?'
