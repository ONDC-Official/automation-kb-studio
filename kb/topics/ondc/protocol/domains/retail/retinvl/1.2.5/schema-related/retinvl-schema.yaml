# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: retinvl-schema
path:
  - ondc
  - protocol
  - domains
  - retail
  - retinvl
  - "1.2.5"
  - schema-related
title: 'ONDC:RETINVL 1.2.5 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:RETINVL version 1.2.5?'
  - 'How is the data schema defined for ONDC:RETINVL version 1.2.5?'
  - 'What schema does ONDC:RETINVL version 1.2.5 use?'
