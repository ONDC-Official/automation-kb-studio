# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: retinvl-error-codes
path:
  - ondc
  - protocol
  - domains
  - retail
  - retinvl
  - "1.2.5"
  - error-code-related
title: 'ONDC:RETINVL 1.2.5 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:RETINVL version 1.2.5?'
  - 'Which error codes can ONDC:RETINVL version 1.2.5 return?'
  - 'What are the error codes used in ONDC:RETINVL version 1.2.5?'
