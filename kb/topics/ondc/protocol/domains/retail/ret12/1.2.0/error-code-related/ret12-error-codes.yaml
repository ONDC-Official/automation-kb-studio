# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: ret12-error-codes
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret12
  - "1.2.0"
  - error-code-related
title: 'ONDC:RET12 1.2.0 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:RET12 version 1.2.0?'
  - 'Which error codes can ONDC:RET12 version 1.2.0 return?'
  - 'What are the error codes used in ONDC:RET12 version 1.2.0?'
