# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fashion-transactions
path:
  - ondc
  - protocol
  - domains
  - retail
  - ret12
  - "1.2.5"
  - fashion
  - transaction-related
title: 'ONDC:RET12 1.2.5 — FASHION transactions'
kind: real
questions:
  - 'What is the transaction flow for the FASHION use case in ONDC:RET12 version 1.2.5?'
  - 'How does the transaction flow work for the FASHION use case in ONDC:RET12 version 1.2.5?'
  - 'What are the transaction steps for the FASHION use case in ONDC:RET12 version 1.2.5?'
