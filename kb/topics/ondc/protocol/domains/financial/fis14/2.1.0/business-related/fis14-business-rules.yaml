# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis14-business-rules
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis14
  - "2.1.0"
  - business-related
title: 'ONDC:FIS14 2.1.0 — business rules'
kind: real
questions:
  - 'What are the business rules for ONDC:FIS14 version 2.1.0?'
  - 'Which business rules apply to ONDC:FIS14 version 2.1.0?'
  - 'What business rules govern ONDC:FIS14 version 2.1.0?'
