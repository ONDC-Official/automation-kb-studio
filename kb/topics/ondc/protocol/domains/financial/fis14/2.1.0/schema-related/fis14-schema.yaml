# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis14-schema
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis14
  - "2.1.0"
  - schema-related
title: 'ONDC:FIS14 2.1.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:FIS14 version 2.1.0?'
  - 'How is the data schema defined for ONDC:FIS14 version 2.1.0?'
  - 'What schema does ONDC:FIS14 version 2.1.0 use?'
