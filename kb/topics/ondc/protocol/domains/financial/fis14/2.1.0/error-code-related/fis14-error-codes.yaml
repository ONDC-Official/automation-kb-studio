# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis14-error-codes
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis14
  - "2.1.0"
  - error-code-related
title: 'ONDC:FIS14 2.1.0 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:FIS14 version 2.1.0?'
  - 'Which error codes can ONDC:FIS14 version 2.1.0 return?'
  - 'What are the error codes used in ONDC:FIS14 version 2.1.0?'
