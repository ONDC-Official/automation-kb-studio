# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: gift-card-transactions
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis10
  - "2.1.0"
  - gift-card
  - transaction-related
title: 'ONDC:FIS10 2.1.0 — gift-card transactions'
kind: real
questions:
  - 'What is the transaction flow for the gift-card use case in ONDC:FIS10 version 2.1.0?'
  - 'How does the transaction flow work for the gift-card use case in ONDC:FIS10 version 2.1.0?'
  - 'What are the transaction steps for the gift-card use case in ONDC:FIS10 version 2.1.0?'
