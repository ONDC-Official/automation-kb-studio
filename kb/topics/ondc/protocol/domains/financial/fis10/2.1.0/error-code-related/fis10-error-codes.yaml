# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis10-error-codes
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis10
  - "2.1.0"
  - error-code-related
title: 'ONDC:FIS10 2.1.0 — error codes'
kind: real
questions:
  - 'What error codes are defined for ONDC:FIS10 version 2.1.0?'
  - 'Which error codes can ONDC:FIS10 version 2.1.0 return?'
  - 'What are the error codes used in ONDC:FIS10 version 2.1.0?'
