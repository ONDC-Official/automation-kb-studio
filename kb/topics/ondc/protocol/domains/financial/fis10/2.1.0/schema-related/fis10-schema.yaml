# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis10-schema
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis10
  - "2.1.0"
  - schema-related
title: 'ONDC:FIS10 2.1.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:FIS10 version 2.1.0?'
  - 'How is the data schema defined for ONDC:FIS10 version 2.1.0?'
  - 'What schema does ONDC:FIS10 version 2.1.0 use?'
