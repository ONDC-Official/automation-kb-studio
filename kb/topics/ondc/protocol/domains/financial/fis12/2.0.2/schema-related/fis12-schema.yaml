# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis12-schema
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis12
  - "2.0.2"
  - schema-related
title: 'ONDC:FIS12 2.0.2 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:FIS12 version 2.0.2?'
  - 'How is the data schema defined for ONDC:FIS12 version 2.0.2?'
  - 'What schema does ONDC:FIS12 version 2.0.2 use?'
