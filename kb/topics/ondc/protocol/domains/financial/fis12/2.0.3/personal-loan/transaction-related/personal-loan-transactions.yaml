# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: personal-loan-transactions
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis12
  - "2.0.3"
  - personal-loan
  - transaction-related
title: 'ONDC:FIS12 2.0.3 — PERSONAL LOAN transactions'
kind: real
questions:
  - 'What is the transaction flow for the PERSONAL LOAN use case in ONDC:FIS12 version 2.0.3?'
  - 'How does the transaction flow work for the PERSONAL LOAN use case in ONDC:FIS12 version 2.0.3?'
  - 'What are the transaction steps for the PERSONAL LOAN use case in ONDC:FIS12 version 2.0.3?'
