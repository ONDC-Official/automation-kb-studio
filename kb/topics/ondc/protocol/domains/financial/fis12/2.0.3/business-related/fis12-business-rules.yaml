# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis12-business-rules
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis12
  - "2.0.3"
  - business-related
title: 'ONDC:FIS12 2.0.3 — business rules'
kind: real
questions:
  - 'What are the business rules for ONDC:FIS12 version 2.0.3?'
  - 'Which business rules apply to ONDC:FIS12 version 2.0.3?'
  - 'What business rules govern ONDC:FIS12 version 2.0.3?'
