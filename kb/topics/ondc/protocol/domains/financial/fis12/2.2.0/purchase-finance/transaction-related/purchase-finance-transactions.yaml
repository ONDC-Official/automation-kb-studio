# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: purchase-finance-transactions
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis12
  - "2.2.0"
  - purchase-finance
  - transaction-related
title: 'ONDC:FIS12 2.2.0 — PURCHASE FINANCE transactions'
kind: real
questions:
  - 'What is the transaction flow for the PURCHASE FINANCE use case in ONDC:FIS12 version 2.2.0?'
  - 'How does the transaction flow work for the PURCHASE FINANCE use case in ONDC:FIS12 version 2.2.0?'
  - 'What are the transaction steps for the PURCHASE FINANCE use case in ONDC:FIS12 version 2.2.0?'
