# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: metro-card-recharge-transactions
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis11
  - "2.0.0"
  - metro-card-recharge
  - transaction-related
title: 'ONDC:FIS11 2.0.0 — metro-card-recharge transactions'
kind: real
questions:
  - 'What is the transaction flow for the metro-card-recharge use case in ONDC:FIS11 version 2.0.0?'
  - 'How does the transaction flow work for the metro-card-recharge use case in ONDC:FIS11 version 2.0.0?'
  - 'What are the transaction steps for the metro-card-recharge use case in ONDC:FIS11 version 2.0.0?'
