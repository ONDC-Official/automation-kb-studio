# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: transit-insurance-transactions
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis13
  - "2.0.0"
  - transit-insurance
  - transaction-related
title: 'ONDC:FIS13 2.0.0 — transit-insurance transactions'
kind: real
questions:
  - 'What is the transaction flow for the transit-insurance use case in ONDC:FIS13 version 2.0.0?'
  - 'How does the transaction flow work for the transit-insurance use case in ONDC:FIS13 version 2.0.0?'
  - 'What are the transaction steps for the transit-insurance use case in ONDC:FIS13 version 2.0.0?'
