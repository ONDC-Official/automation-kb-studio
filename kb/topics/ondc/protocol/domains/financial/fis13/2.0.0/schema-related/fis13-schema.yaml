# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: fis13-schema
path:
  - ondc
  - protocol
  - domains
  - financial
  - fis13
  - "2.0.0"
  - schema-related
title: 'ONDC:FIS13 2.0.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:FIS13 version 2.0.0?'
  - 'How is the data schema defined for ONDC:FIS13 version 2.0.0?'
  - 'What schema does ONDC:FIS13 version 2.0.0 use?'
