# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: nts10-schema
path:
  - ondc
  - protocol
  - domains
  - settlement
  - nts10
  - "2.0.0"
  - schema-related
title: 'ONDC:NTS10 2.0.0 — schema'
kind: real
questions:
  - 'What is the schema for ONDC:NTS10 version 2.0.0?'
  - 'How is the data schema defined for ONDC:NTS10 version 2.0.0?'
  - 'What schema does ONDC:NTS10 version 2.0.0 use?'
