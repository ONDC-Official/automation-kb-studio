# Real topic — a genuine topic the source SHOULD be able to answer. Equivalent phrasings: the
# coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so vary the
# wording, not the meaning.
id: reconciliation-transactions
path:
  - ondc
  - protocol
  - domains
  - settlement
  - nts10
  - "2.0.0"
  - reconciliation
  - transaction-related
title: 'ONDC:NTS10 2.0.0 — RECONCILIATION transactions'
kind: real
questions:
  - 'What is the transaction flow for the RECONCILIATION use case in ONDC:NTS10 version 2.0.0?'
  - 'How does the transaction flow work for the RECONCILIATION use case in ONDC:NTS10 version 2.0.0?'
  - 'What are the transaction steps for the RECONCILIATION use case in ONDC:NTS10 version 2.0.0?'
